# Connection to your existing Northwind MariaDB/MySQL server (already
# running elsewhere on your network, not managed by this docker-compose
# stack). Copy this file to .env and fill in your real values.
DB_HOST=192.168.1.100
DB_PORT=3306
DB_USER=root
DB_PASS=
DB_NAME=northwind
